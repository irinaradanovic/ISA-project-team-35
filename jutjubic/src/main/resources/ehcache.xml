<?xml version="1.0" encoding="UTF-8"?>
<config
        xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xsi:schemaLocation="http://www.ehcache.org/v3
        http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <!-- Persistent cache directory -->
    <persistence directory="cache/thumbnails" />

    <!-- Default cache template -->
    <cache-template name="defaultThumbnailTemplate">
        <!-- TTL - time to live: 30 minuta -->
        <expiry>
            <ttl unit="seconds">10</ttl>
        </expiry>

        <!-- Listener za logging -->
        <listeners>
            <listener>
                <class>com.isa.jutjubic.logger.ThumbnailCacheLogger</class>
                <event-firing-mode>ASYNCHRONOUS</event-firing-mode>
                <event-ordering-mode>UNORDERED</event-ordering-mode>
                <events-to-fire-on>CREATED</events-to-fire-on>
                <events-to-fire-on>EXPIRED</events-to-fire-on>
                <events-to-fire-on>EVICTED</events-to-fire-on>
                <events-to-fire-on>REMOVED</events-to-fire-on>
            </listener>
        </listeners>

        <resources>
            <!-- 100 objekata u memoriji -->
            <heap unit="entries">100</heap>
            <!-- 10 MB offheap -->
            <offheap unit="MB">10</offheap>
            <!-- 50 MB na disku -->
            <disk persistent="true" unit="MB">50</disk>
        </resources>
    </cache-template>

    <!-- Cache konkretno za thumbnails -->
    <cache alias="thumbnails" uses-template="defaultThumbnailTemplate">
        <key-type>java.lang.String</key-type>
        <value-type>byte[]</value-type>
        <resources>
            <heap unit="entries">100</heap>
        </resources>
    </cache>

</config>
